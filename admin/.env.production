# ============================================================
# Admin — Production Environment Variables (Vercel)
# ============================================================
# Set these in Vercel Dashboard → Settings → Environment Variables
#
# VITE_API_BASE must be the FULL URL to the backend API.
# In production, the API lives at api.galaxytv4k.online (via Cloudflare Tunnel).
# ============================================================

VITE_API_BASE=https://api.galaxytv4k.online
